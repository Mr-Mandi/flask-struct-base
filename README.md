# Estructura Base para proyecto en Flask python

Estructura base para crear un proyecto 

<hr/>

Primero, crear un entorno virtual:
### `python -m venv venv`

Para instalar los paquetes necesarios:
### `pip install -r requirements.txt`
